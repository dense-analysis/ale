Before:
  Save g:ale_hcl_hclfmt_executable

  " Use an invalid global executable, so we don't match it.
  let g:ale_hcl_hclfmt_executable = 'xxxinvalid'

  call ale#test#SetDirectory('/testplugin/test/fixers')

After:
  Restore

  call ale#test#RestoreDirectory()

Execute(The hclfmt callback should return the correct default values):
  call ale#test#SetFilename('../hcl_files/testfile.hcl')

  AssertEqual
  \ {
  \   'read_temporary_file': 1,
  \   'command': ale#Escape('xxxinvalid')
  \     . ' -w'
  \     . ' %t',
  \ },
  \ ale#fixers#hclfmt#Fix(bufnr(''))
