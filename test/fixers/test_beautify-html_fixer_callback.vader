Before:
  Save g:beautify_html_executable
  Save g:beautify_html_options

  " Use an invalid global executable, so we don't match it.
  let g:ale_beautify_html_executable = 'xxxinvalid'
  let g:ale_beautify_html_options = ''

  call ale#test#SetDirectory('/testplugin/test/fixers')

After:
  Restore

  call ale#test#RestoreDirectory()

Execute(The beautify_html callback should return the correct default values):
  call ale#test#SetFilename('../beautify_files/dummy.html')

  AssertEqual
  \ {
  \   'command': 'xxxinvalid'
  \ },
  \ ale#fixers#beautify_html#Fix(bufnr(''))

Execute(The beautify_html callback should include custom beautify_html options):
  let g:ale_beautify_html_options = ' -S '

  call ale#test#SetFilename('../beautify_files/dummy.html')

  AssertEqual
  \ {
  \   'command': 'xxxinvalid' . g:ale_beautify_html_options
  \ },
  \ ale#fixers#beautify_html#Fix(bufnr(''))
