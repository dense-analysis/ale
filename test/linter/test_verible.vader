Before:
  call ale#assert#SetUpLinterTest('verilog', 'verible')

After:
  call ale#assert#TearDownLinterTest()

Execute(The default iverilog command should be correct):
  AssertLinter 'iverilog', 'verible-verilog-lint '

Execute(iverilog options should be configurable):
  " Additional args for the linter
  let g:ale_verilog_verible_options = '--ruleset=all '

  AssertLinter 'verible', 'verible-verilog-lint --ruleset=all %t'
